# ==========================================
# Required API Keys
# ==========================================

# OpenAI API Key (required for LLM functionality)
OPENAI_API_KEY=sk-your-openai-api-key-here

# Agent API Key (for authentication - set to secure random string)
# Generate with: openssl rand -base64 32
AGENT_API_KEY=your-secure-api-key-here

# ==========================================
# Model Configuration (Optional)
# ==========================================

# LLM model to use (default: gpt-4o-mini)
# Options: gpt-4o-mini, gpt-4o, gpt-4-turbo, gpt-3.5-turbo
MODEL_NAME=gpt-4o-mini

# Model temperature (default: 0)
# Range: 0-2, lower = more deterministic
TEMPERATURE=0

# ==========================================
# LangSmith Configuration (Optional)
# For tracing and debugging LangChain applications
# ==========================================

LANGSMITH_API_KEY=
LANGSMITH_PROJECT=hyperliquid-agent
LANGSMITH_TRACING=false

# ==========================================
# Database Configuration (Optional)
# Only required when using docker-compose.full.yml
# ==========================================

# PostgreSQL password (used in full deployment)
POSTGRES_PASSWORD=langgraph_password
